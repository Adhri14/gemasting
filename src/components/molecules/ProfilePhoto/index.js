import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ProfilePhoto = () => {
  return (
    <View>
      <Text>Profle Photo</Text>
    </View>
  );
};

export default ProfilePhoto;

const styles = StyleSheet.create({});
